# snack

a stupid fast and lightweight Slack client built with Rust and [Iced](https://book.iced.rs/the-runtime.html)

## run

clone the repo locally and run the following command to start the app:

```sh
cargo run
```
